import 'package:flutter/material.dart';

class ItemUserNewsFeed extends StatelessWidget {
  const ItemUserNewsFeed({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
